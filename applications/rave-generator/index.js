const php = require('phpjs');const redis = require("redis");var environment = php.ksort(process.env);// Connect to REDISvar redisSender = redis.createClient({    host: environment.REDIS_PORT_6379_TCP_ADDR,    port: environment.REDIS_PORT_6379_TCP_PORT});raveDelay = 1500;var rave = function(){    var request = {        scope: 'all',        colour: {            red: Math.random(),            green: Math.random(),            blue: Math.random(),            brightness: 1.0        }    };    console.log("Rave!");    redisSender.publish('lights_request', JSON.stringify(request));};setInterval(rave, raveDelay);